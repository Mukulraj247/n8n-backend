{
  "name": "backend-n8n",
  "version": "1.0.0",
  "description": "Headless-browser auth microservice for the n8n JobRight ingestion workflow. Logs into JobRight with Playwright and serves a fresh SESSION_ID cookie.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "login:test": "node src/testLogin.js",
    "postinstall": "playwright install chromium"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "playwright": "^1.47.2"
  }
}
